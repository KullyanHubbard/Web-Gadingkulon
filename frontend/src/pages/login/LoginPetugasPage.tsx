import { LoginPetugasForm } from '@/features/auth/components/LoginPetugasForm';

/** Masuk khusus pengurus padukuhan (Dukuh / RW / RT). */
export default function LoginPetugasPage() {
  return <LoginPetugasForm />;
}
